(function() {
    
    /**
     * applicationModule
     *
     * The angular modules definition.  This module provides all functionality
     * related to running CBT content (To-Do List and Launchpage).  For now this
     * module is derived directly from the legacy LMS practices of ATMS.
     */
    angular.module('lms', []);

}) ();
