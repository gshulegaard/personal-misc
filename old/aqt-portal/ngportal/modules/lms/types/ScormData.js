(function() {
    
    /**
     * ScormData
     * 
     * This factory defines a re-usable ScormData type that is used to store
     * SCORM data while e-learning content is run in the launchpage.html.  This
     * type also has methods for sending data to the API.
     */
    angular.module('lms').factory('ScormData', [
        // Dependencies go here..
        function() {
            var Type = function(template) {
                // Use an empty template by default.
                template = template || {};
            };

            return type;
        }
 
    ]);

}) ();
