(function() {
    
    /**
     * todoController
     *
     * The controller definition for the lms To-Do list.
     */
    angular.module('lms').controller('todoController', [
        // Dependencies go here...
        function() {
            // Do nothing for now.
        }

    ]);

}) ();
